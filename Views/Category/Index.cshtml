@model dotNetCoreEntityFramework.Domain.Category
@using dotNetCoreEntityFramework.Domain
@{
    ViewData["Title"] = "Cadastro de Gategoria";
}


@foreach(var item in ViewBag.Categories as List<Category>)
{
    <li>
        @item.Name
        <a href="/Category/Index/@item.Id">Editar</a>

        <form asp-action="Delete" asp-controller="Category" method="POST">
            <input type="hidden" name="id" value="@item.Id">
            <button type="submit" class="btn btn-default">Deletar</button>
        </form>


    </li>
}


<form asp-action="Register" asp-controller="Category" method="POST">

    <input hidden="hidden" asp-for="Id">

    <div class="form-group">
        <label for="Name">Nome</label>
        <input type="text" class="form-control" asp-for="Name">
    </div>

    <button type="submit" class="btn btn-default">Salvar</button>

</form>